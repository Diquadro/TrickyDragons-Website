mixin cta_modal(id, title, message)
    .cta-modal-overlay(id=id)
        .cta-modal-content
            h2= title
            p= message
            br
            button.action-button Keep Exploring
